#include <stdio.h>
/**
 * main - print all numbers between 0 and 10
 * Return: 0
*/
int main(void)
{
	int number;

	for (number = 0; number < 10; number++)
		printf("%d", number);
	printf("\n");
	return (0);
}
