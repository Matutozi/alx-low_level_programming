#include "main.h"

/* mul - to multiply two digits together
 * @a: parameter to use
 * @b: parameter to use
 * Return: return 0 when sucessful
*/

int mul(int a, int b)
{
	int cal;

	cal = a * b;
	return (cal);
}
