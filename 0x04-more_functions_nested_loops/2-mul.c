#include "main.h"
/**
 * mul - checks for checks for a digit from (0 through 9).
 * @a: a - Variable to be checked
 * @b: b - variable to be checked
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
